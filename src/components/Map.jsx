// Map.js
import React from "react";

const Map = ({ location }) => {
  return (
    <div className="map">
      {/* Use a map library (e.g. Google Maps, Leaflet, etc.) to display the map */}
    </div>
  );
};

export default Map;